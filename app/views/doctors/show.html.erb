<h2>Appointments</h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.format_date %></li>
  <% end %>
</ul>
<h2>Patients</h2>
<ul>
  <% @doctor.patients.each do |patient| %>
    <li><%= link_to patient.name, patient_path(patient) %></li>
  <% end %>
</ul>
