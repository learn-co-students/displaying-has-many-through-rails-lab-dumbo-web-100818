<h1><%= @doctor.name %></h1>
Appointments:
<% @doctor.appointments.each do |appointment| %>
<%=link_to appointment.patient.name, appointment.patient %> <%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M')%>
<% end %>
